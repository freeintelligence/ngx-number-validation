import { DecimalsDirective } from './decimals.directive';

describe('DecimalsDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalsDirective();
    expect(directive).toBeTruthy();
  });
});
