import { MinLimitDirective } from './min-limit.directive';

describe('MinLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new MinLimitDirective();
    expect(directive).toBeTruthy();
  });
});
