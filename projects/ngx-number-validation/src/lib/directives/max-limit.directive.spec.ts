import { MaxLimitDirective } from './max-limit.directive';

describe('MaxLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxLimitDirective();
    expect(directive).toBeTruthy();
  });
});
