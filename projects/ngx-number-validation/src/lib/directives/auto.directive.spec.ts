import { AutoDirective } from './auto.directive';

describe('AutoDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoDirective();
    expect(directive).toBeTruthy();
  });
});
